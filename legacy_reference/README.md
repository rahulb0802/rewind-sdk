These scripts are preserved as legacy references for the SDK rebuild.

They are not part of the active test suite. Their simulation sequences remain useful
as behavioral baselines for the package-first Rewind SDK tests in `tests/`.
