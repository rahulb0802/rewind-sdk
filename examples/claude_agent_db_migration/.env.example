# Auth for the Claude Agent SDK (either works — the SDK picks whichever is set)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# CLAUDE_CODE_OAUTH_TOKEN=your_oauth_token_here

# Optional model override (defaults to the Agent SDK's built-in default model)
CLAUDE_MODEL=
