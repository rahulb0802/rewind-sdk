[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "rewind_sdk"
version = "0.2.0"
description = "Transactional runtime for AI agents"
dependencies = []

[tool.setuptools]
packages = ["rewind_sdk"]